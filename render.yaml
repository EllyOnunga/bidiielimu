services:
  - type: web
    name: elimuhub-backend
    runtime: python
    region: oregon
    plan: free
    buildCommand: "cd backend && pip install -r requirements.txt && python manage.py collectstatic --no-input"
    startCommand: "cd backend && python manage.py migrate --noinput && python manage.py create_public_tenant && gunicorn config.wsgi:application -b 0.0.0.0:$PORT --timeout 120 --workers 1 --threads 2"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.13
      - key: DATABASE_URL
        fromDatabase:
          name: elimuhub-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: .onrender.com,localhost,127.0.0.1
      - key: DEBUG
        value: "True"
      - key: WEB_CONCURRENCY
        value: 1

databases:
  - name: elimuhub-db
    plan: free
    region: oregon
